# CMakeLists.txt
# By Sebastian Raaphorst, 2018.

set(_TYPECLASSES_PUBLIC_HEADER_FILES
        Homomorphism.h
        Show.h
        PARENT_SCOPE
        )

set(_TYPECLASSES_PRIVATE_HEADER_FILES
        PARENT_SCOPE
        )

set(_TYPECLASSES_SOURCE_FILES
        PARENT_SCOPE
        )

